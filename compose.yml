services:
  blog:
    container_name: "blog"
    build:
      dockerfile: ./Dockerfile
      target: run
    ports:
      - '3000:3000'
    environment:
      VITE_MAPBOX_API_KEY: ${VITE_MAPBOX_API_KEY}